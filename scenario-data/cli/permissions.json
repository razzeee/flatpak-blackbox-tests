{
  "schema": 1,
  "behaviors": [
    {
      "id": "permissions.selection",
      "scenario": "permissions-selection",
      "handler": "permission_scenarios:run",
      "drivers": [
        "cli"
      ],
      "profile": "any",
      "required_capabilities": [
        "cli",
        "desktop-portals",
        "session-bus"
      ],
      "description": "Enumerate all entries and select a table or object.",
      "preconditions": "Two apps across three objects in two tables, including repeated object IDs.",
      "expected": "Exact row sets for all, table-only, and table/object queries exclude unrelated entries.",
      "references": [
        "doc/flatpak-permissions.xml"
      ]
    }
  ],
  "mappings": [
    {
      "behavior_id": "permissions.selection",
      "driver": "cli",
      "profile": "any",
      "requirements": [
        "cli.permissions.any.selection"
      ],
      "rationale": "Exact table/object result rows exclude the other object in that table and identically named object in another table; broader queries have distinct expected row sets."
    }
  ],
  "notes": [
    "Uses a real xdg-permission-store on the case bus with isolated XDG_DATA_HOME; gdbus provides independent public fixture setup and mutation observations. No FUSE or document portal is needed.",
    "All service output and termination status are retained, including failed cases. Fixed catalogues and denominators are unchanged."
  ]
}
