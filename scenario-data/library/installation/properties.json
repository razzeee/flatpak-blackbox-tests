{
  "schema": 1,
  "behaviors": [
    {
      "id": "objects.installation-identity",
      "scenario": "objects-installation-identity",
      "handler": "library_objects_scenarios:run",
      "drivers": [
        "library"
      ],
      "profile": "user",
      "required_capabilities": [
        "libflatpak",
        "user-installation"
      ],
      "description": "Select the user installation and retain its owned path.",
      "preconditions": "Runner-controlled HOME/XDG environment.",
      "expected": "is_user TRUE, ID user, path equals configured user location and remains usable after installation release.",
      "references": [
        "common/flatpak-installation.c: flatpak_installation_new_user, flatpak_installation_get_path, flatpak_installation_get_id"
      ]
    },
    {
      "id": "objects.custom-path",
      "scenario": "objects-custom-path",
      "handler": "library_objects_scenarios:run",
      "drivers": [
        "library"
      ],
      "profile": "user",
      "required_capabilities": [
        "libflatpak",
        "user-installation",
        "multiple-installations",
        "local-repository"
      ],
      "description": "Keep custom user installation queries separate from default user state.",
      "preconditions": "Fixture app/runtime installed in default user location; distinct empty custom path.",
      "expected": "Custom constructor reports its supplied path and TRUE user profile; its ref list is empty while default list is exactly the app/runtime; custom configuration survives reopening.",
      "references": [
        "common/flatpak-installation.c: flatpak_installation_new_for_path, flatpak_installation_list_installed_refs"
      ]
    },
    {
      "id": "objects.remote-cache",
      "scenario": "objects-remote-cache",
      "handler": "library_objects_scenarios:run",
      "drivers": [
        "library"
      ],
      "profile": "user",
      "required_capabilities": [
        "libflatpak",
        "user-installation"
      ],
      "description": "Refresh remote configuration cached by an installation.",
      "preconditions": "A second installation object commits a different URL.",
      "expected": "Dropping original object's caches succeeds and its next lookup returns the changed literal URL.",
      "references": [
        "common/flatpak-installation.c: flatpak_installation_drop_caches"
      ]
    }
  ],
  "mappings": [
    {
      "behavior_id": "objects.installation-identity",
      "driver": "library",
      "profile": "user",
      "requirements": [
        "library.installation.user.identity",
        "library.ownership.installation-path"
      ],
      "rationale": "User profile, ID and independently expected path are checked; owned GFile is used and released after installation destruction."
    },
    {
      "behavior_id": "objects.custom-path",
      "driver": "library",
      "profile": "user",
      "requirements": [
        "library.installation.user.custom-path"
      ],
      "rationale": "Custom path/user identity and empty queries contrast with the exact two installed default-user refs."
    },
    {
      "behavior_id": "objects.remote-cache",
      "driver": "library",
      "profile": "user",
      "requirements": [
        "library.installation.cache-refresh"
      ],
      "rationale": "Second installation commits changed URL; original installation drops caches and observes that literal change."
    }
  ],
  "client": {
    "source": "client-objects.c",
    "entry": "blackbox_objects_main"
  }
}
