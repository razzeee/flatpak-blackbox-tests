{
  "schema": 1,
  "behaviors": [
    {
      "id": "related-options.install",
      "scenario": "related-options-install",
      "handler": "related_option_scenarios:run",
      "drivers": [
        "cli"
      ],
      "profile": "user",
      "required_capabilities": [
        "user-installation",
        "local-repository"
      ],
      "description": "Suppress related locale installation while retaining required runtime dependencies.",
      "preconditions": "Independent selection app, runtime and autodelete locale extension; explicit de language.",
      "expected": "Install --no-related deploys only app and runtime. Ordinary installation also deploys the exact locale commit.",
      "references": [
        "doc/flatpak-install.xml"
      ]
    },
    {
      "id": "management.install-arch",
      "scenario": "management-install-arch",
      "handler": "cli_management_scenarios:run",
      "drivers": [
        "cli"
      ],
      "profile": "user",
      "required_capabilities": [
        "user-installation",
        "local-repository"
      ],
      "description": "Install foreign architecture using an ambiguous architecture-free name.",
      "preconditions": "queries fixtures with native and foreign copies.",
      "expected": "Only the exact foreign commit is deployed.",
      "references": [
        "doc/flatpak-install.xml"
      ]
    },
    {
      "id": "management.install-kinds",
      "scenario": "management-install-kinds",
      "handler": "cli_management_scenarios:run",
      "drivers": [
        "cli"
      ],
      "profile": "user",
      "required_capabilities": [
        "user-installation",
        "local-repository"
      ],
      "description": "Reject opposite-kind candidates when installing by ID.",
      "preconditions": "Available app/runtime refs and empty installation.",
      "expected": "App-only rejects runtime ID; runtime-only rejects app ID; no deployment.",
      "references": [
        "doc/flatpak-install.xml"
      ]
    },
    {
      "id": "management.flatpakref",
      "scenario": "management-flatpakref",
      "handler": "cli_management_scenarios:run",
      "drivers": [
        "cli"
      ],
      "profile": "user",
      "required_capabilities": [
        "user-installation",
        "local-repository"
      ],
      "description": "Install an application description.",
      "preconditions": "Fresh state and A repository.",
      "expected": "Described app and dependency commits installed.",
      "references": [
        "doc/flatpak-install.xml"
      ]
    },
    {
      "id": "management.install-local",
      "scenario": "management-install-local",
      "handler": "cli_management_scenarios:run",
      "drivers": [
        "cli"
      ],
      "profile": "user",
      "required_capabilities": [
        "user-installation",
        "local-repository"
      ],
      "description": "Download A without deployment, then deploy locally with B advertised.",
      "preconditions": "Fresh state and A/B repositories.",
      "expected": "No refs installed after download; local deployment selects A.",
      "references": [
        "doc/flatpak-install.xml"
      ]
    },
    {
      "id": "management.install-no-deps",
      "scenario": "management-install-no-deps",
      "handler": "cli_management_scenarios:run",
      "drivers": [
        "cli"
      ],
      "profile": "user",
      "required_capabilities": [
        "user-installation",
        "local-repository"
      ],
      "description": "Install app while suppressing dependency installation.",
      "preconditions": "Fresh state and A repository.",
      "expected": "Only application A is deployed.",
      "references": [
        "doc/flatpak-install.xml"
      ]
    },
    {
      "id": "management.or-update",
      "scenario": "management-or-update",
      "handler": "cli_management_scenarios:run",
      "drivers": [
        "cli"
      ],
      "profile": "user",
      "required_capabilities": [
        "user-installation",
        "local-repository"
      ],
      "description": "Install again with or-update.",
      "preconditions": "A installed, B advertised.",
      "expected": "Installed app changes to B.",
      "references": [
        "doc/flatpak-install.xml"
      ]
    },
    {
      "id": "management.reinstall",
      "scenario": "management-reinstall",
      "handler": "cli_management_scenarios:run",
      "drivers": [
        "cli"
      ],
      "profile": "user",
      "required_capabilities": [
        "user-installation",
        "local-repository"
      ],
      "description": "Reinstall existing app with newer tip available.",
      "preconditions": "A installed, B advertised.",
      "expected": "Reinstall replaces A with B.",
      "references": [
        "doc/flatpak-install.xml"
      ]
    },
    {
      "id": "coverage.bundle",
      "scenario": "coverage-bundle",
      "handler": "coverage_next_scenarios:run",
      "drivers": [
        "cli"
      ],
      "profile": "user",
      "required_capabilities": [
        "local-repository"
      ],
      "description": "Install an unsigned single-file bundle with explicit format selection.",
      "preconditions": "Required runtime installed; bundle copied to an extensionless case-local filename; bundle app absent and unavailable from configured remote.",
      "expected": "Without --bundle the input fails without deployment; with --bundle its exact app commit is installed, runtime preserved and companion absent.",
      "references": [
        "doc/flatpak-install.xml"
      ]
    },
    {
      "id": "system-selectors.lifecycle",
      "scenario": "system-selectors-lifecycle",
      "handler": "system_selector_scenarios:run",
      "profile": "system",
      "execution_environment": "root-userns",
      "drivers": [
        "cli"
      ],
      "required_capabilities": [
        "system-installation",
        "user-installation",
        "multiple-installations",
        "local-repository"
      ],
      "description": "Install default, named and user copies, update each independently, then remove each independently.",
      "preconditions": "Direct namespace-root access to private system stores; immutable A/B repositories.",
      "expected": "Implicit install selects the actual default path; each update/removal leaves all other copies at their expected commits.",
      "references": [
        "doc/flatpak-install.xml",
        "doc/flatpak-update.xml",
        "doc/flatpak-uninstall.xml",
        "doc/flatpak-info.xml"
      ]
    },
    {
      "id": "system-selectors.install-options",
      "scenario": "system-selectors-install-options",
      "handler": "system_selector_scenarios:run",
      "profile": "system",
      "execution_environment": "root-userns",
      "drivers": [
        "cli"
      ],
      "required_capabilities": [
        "system-installation",
        "user-installation",
        "multiple-installations",
        "local-repository"
      ],
      "description": "Explicit system reinstall after removal, or-update, and reinstall of an older installed version.",
      "preconditions": "Independent default A, named B, and user A deployments.",
      "expected": "--system recreates the removed default copy; or-update and reinstall reach B while user A survives.",
      "references": [
        "doc/flatpak-install.xml"
      ]
    }
  ],
  "mappings": [
    {
      "behavior_id": "related-options.install",
      "driver": "cli",
      "profile": "user",
      "requirements": [],
      "rationale": "Exact ref sets and independent commits contrast suppressed and ordinary related-ref installation.",
      "surface_assertions": [
        {
          "id": "cli.option.install.--no-related",
          "rationale": "The locale is absent only with the flag, while the required runtime remains installed in both runs."
        }
      ]
    },
    {
      "behavior_id": "management.install-arch",
      "driver": "cli",
      "profile": "user",
      "requirements": [],
      "rationale": "Competing architecture refs make ignoring selector observable.",
      "surface_assertions": [
        {
          "id": "cli.option.install.--arch",
          "rationale": "Foreign-only installed ref set differs from available native default."
        }
      ]
    },
    {
      "behavior_id": "management.install-kinds",
      "driver": "cli",
      "profile": "user",
      "requirements": [],
      "rationale": "Opposite-kind available candidates must not be installed.",
      "surface_assertions": [
        {
          "id": "cli.option.install.--app",
          "rationale": "App-only installation rejects an available runtime ID without deployment."
        },
        {
          "id": "cli.option.install.--runtime",
          "rationale": "Runtime-only installation rejects an available app ID without deployment."
        }
      ]
    },
    {
      "behavior_id": "management.flatpakref",
      "driver": "cli",
      "profile": "user",
      "requirements": [
        "cli.install.user.flatpakref"
      ],
      "rationale": "Description-driven installation deploys exact A app and runtime commits."
    },
    {
      "behavior_id": "management.install-local",
      "driver": "cli",
      "profile": "user",
      "requirements": [
        "cli.install.user.download-only",
        "cli.install.user.local-deploy"
      ],
      "rationale": "Download leaves queries empty; no-pull installs A despite remote B."
    },
    {
      "behavior_id": "management.install-no-deps",
      "driver": "cli",
      "profile": "user",
      "requirements": [
        "cli.install.user.app"
      ],
      "rationale": "Application A installed and queryable with dependency suppression.",
      "surface_assertions": [
        {
          "id": "cli.option.install.--no-deps",
          "rationale": "Exact deployed ref set contains app and excludes its available required runtime."
        }
      ]
    },
    {
      "behavior_id": "management.or-update",
      "driver": "cli",
      "profile": "user",
      "requirements": [
        "cli.install.user.or-update"
      ],
      "rationale": "Repeat install changes exact installed commit A to B."
    },
    {
      "behavior_id": "management.reinstall",
      "driver": "cli",
      "profile": "user",
      "requirements": [
        "cli.install.user.app"
      ],
      "rationale": "Reinstalled application has expected available B commit.",
      "surface_assertions": [
        {
          "id": "cli.option.install.--reinstall",
          "rationale": "Existing A deployment is replaced by B instead of ignored as already installed."
        }
      ]
    },
    {
      "behavior_id": "coverage.bundle",
      "driver": "cli",
      "profile": "user",
      "requirements": [
        "cli.install.user.bundle"
      ],
      "rationale": "An independently exported bundle is copied to an extensionless filename. Installation without --bundle fails without changing installed refs; explicit --bundle installs its exact app commit, preserves the runtime and leaves the companion absent."
    },
    {
      "behavior_id": "system-selectors.lifecycle",
      "driver": "cli",
      "profile": "system",
      "requirements": [
        "cli.install.system.default",
        "cli.install.system.named",
        "cli.update.system.selected",
        "cli.uninstall.system.selected"
      ],
      "rationale": "Direct namespace-root install/update/uninstall outcomes are verified at actual system/named paths, including unchanged user commits and removal-query failure. No non-root authorization claim.",
      "surface_assertions": [
        {
          "id": "cli.option.install.--installation",
          "rationale": "Named install creates B under alt while default remains A and user remains empty."
        },
        {
          "id": "cli.option.install.--user",
          "rationale": "User install creates A under user path while named B and default A remain separate."
        },
        {
          "id": "cli.option.update.--system",
          "rationale": "Default A changes to B while separately installed user A remains unchanged."
        },
        {
          "id": "cli.option.update.--installation",
          "rationale": "Named B downgrades to A while default B and user A are retained."
        },
        {
          "id": "cli.option.update.--user",
          "rationale": "User A changes to B while default B and named A are retained."
        },
        {
          "id": "cli.option.update.--commit",
          "rationale": "Explicit A commit replaces named B even though origin still advertises B."
        },
        {
          "id": "cli.option.uninstall.--system",
          "rationale": "Default app becomes unqueryable while named A and user B survive."
        },
        {
          "id": "cli.option.uninstall.--installation",
          "rationale": "Named app becomes unqueryable while user B survives."
        },
        {
          "id": "cli.option.uninstall.--user",
          "rationale": "User app becomes unqueryable while both competing system app copies and all explicitly installed runtimes remain."
        }
      ]
    },
    {
      "behavior_id": "system-selectors.install-options",
      "driver": "cli",
      "profile": "system",
      "requirements": [],
      "rationale": "Selected install operations are verified through exact commits and untouched user copy.",
      "surface_assertions": [
        {
          "id": "cli.option.install.--system",
          "rationale": "A removed default app is recreated at A while named B and user A remain unchanged."
        },
        {
          "id": "cli.option.install.--or-update",
          "rationale": "Repeat install changes default A to available B while user A survives."
        },
        {
          "id": "cli.option.install.--reinstall",
          "rationale": "Reinstall replaces explicitly downgraded default A with available B while user A survives."
        }
      ]
    }
  ],
  "notes": [
    "Every target call enters two disposable bubblewrap user/mount namespace layers. The outer root maps only to the invoking host UID; inner root maps only to outer root. Inherited host mounts are locked read-only, checked by a denied bind-remount before every target call. Private PID/network namespaces and writable installation state are case-local; generated state is removed after each case while command evidence is retained.",
    "Target environment keys BLACKBOX_SYSTEM_INSTALL_DIR and BLACKBOX_SYSTEM_CONFIG_DIR specify the expected compiled defaults; their reference-build defaults are /var/local/lib/flatpak and /usr/local/etc/flatpak. The namespace mounts private data at those paths, and CLI/C-client assertions still check the target's actual default location. These keys never become FLATPAK_SYSTEM_DIR or FLATPAK_CONFIG_DIR redirects. Set them under the target JSON environment object, not the host shell environment.",
    "Readonly exposure includes only the suite directory, selected target executables, configured helpers and LD_LIBRARY_PATH directories. Uninstalled targets can explicitly add colon-separated absolute paths with BLACKBOX_TARGET_READONLY_PATHS in the target environment. No repository ancestor is inferred. Paths that cover private namespace roots or overlap protected installation/service content are rejected, including symlink aliases.",
    "CLI deployment location checks use the public returned location, verify containment in the selected public installation base, locate the controlled bin/blackbox-probe payload there and execute it to distinguish A from B. They never construct a deployment path from a ref or commit, and never recursively search a general installation root for content.",
    "Direct-root mutations may satisfy explicitly selected installation outcomes. These cases do not assert polkit authorization, non-root helper behavior, or multiple-user access. No requirement demanding those outcomes is mapped.",
    "The named override case intentionally asserts the documented isolation contract; the reference target currently fails it, so that option receives no verified credit from that case."
  ]
}
