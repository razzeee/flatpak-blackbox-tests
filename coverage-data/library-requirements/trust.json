{
  "schema": 1,
  "scope": "library",
  "limitations": [],
  "requirements": [
    {
      "id": "library.remote.trusted-key",
      "interface": "library",
      "profile": "any",
      "category": "trust",
      "description": "Setting and committing a remote's trusted GPG key permits installation of the fixture signed by that key with verification enabled. A corresponding fixture lacking the required trusted key or signature fails instead of deploying unverified content. This obligation does not prescribe whether failure occurs during resolution or operation execution.",
      "sources": [
        {
          "path": "common/flatpak-remote.c",
          "anchor": "flatpak_remote_set_gpg_key"
        },
        {
          "path": "common/flatpak-remote.c",
          "anchor": "flatpak_remote_get_gpg_verify"
        },
        {
          "path": "common/flatpak-error.h",
          "anchor": "FlatpakError"
        }
      ],
      "surfaces": [
        "library.function.flatpak_remote_set_gpg_key",
        "library.function.flatpak_installation_modify_remote",
        "library.function.flatpak_transaction_run"
      ],
      "required_capabilities": [
        "libflatpak",
        "installation",
        "local-repository",
        "repository-signing"
      ]
    }
  ]
}
