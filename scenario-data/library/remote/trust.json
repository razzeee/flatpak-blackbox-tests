{
  "schema": 1,
  "behaviors": [
    {
      "id": "objects.remote-verification",
      "scenario": "objects-remote-verification",
      "handler": "library_objects_scenarios:run",
      "drivers": [
        "library"
      ],
      "profile": "user",
      "required_capabilities": [
        "libflatpak",
        "user-installation"
      ],
      "description": "Persist GPG verification policy.",
      "preconditions": "Remote without a collection ID; verification TRUE then FALSE.",
      "expected": "Both values survive reopening and collection ID remains NULL.",
      "references": [
        "common/flatpak-remote.c: flatpak_remote_set_gpg_verify, flatpak_remote_get_gpg_verify"
      ]
    },
    {
      "id": "contracts.trusted-key",
      "scenario": "remote-trusted-key",
      "handler": "six_contract_scenarios:run",
      "drivers": [
        "library"
      ],
      "profile": "user",
      "required_capabilities": [],
      "description": "Committed trusted key permits signed installation and rejects unverified content.",
      "preconditions": [
        "Signed collection repository, unsigned repository and public key"
      ],
      "expected": [
        "Verification survives reopen; trusted signed commits deploy; missing-key and unsigned controls fail with GError and leave no deployments."
      ],
      "references": [
        "common/flatpak-remote.c:flatpak_remote_set_gpg_key",
        "common/flatpak-transaction.c:flatpak_transaction_run"
      ]
    }
  ],
  "mappings": [
    {
      "behavior_id": "objects.remote-verification",
      "driver": "library",
      "profile": "user",
      "requirements": [
        "library.remote.user.verification-persistence"
      ],
      "rationale": "TRUE then FALSE survive reopening on a verified collection-ID-free remote."
    },
    {
      "behavior_id": "contracts.trusted-key",
      "driver": "library",
      "profile": "user",
      "requirements": [
        "library.remote.trusted-key"
      ],
      "rationale": "Binary public key committed through native setter and modify_remote; reopened verification-enabled remote deploys exact signed commits. Missing-key and missing-signature controls require transaction failure and empty public installed enumeration, without prescribing resolution versus execution failure."
    }
  ]
}
