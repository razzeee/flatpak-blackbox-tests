{
  "schema": 1,
  "behaviors": [
    {
      "id": "maintenance.preinstall-download",
      "scenario": "maintenance-preinstall-download",
      "handler": "maintenance_scenarios:run",
      "profile": "system",
      "execution_environment": "root-userns",
      "drivers": [
        "cli"
      ],
      "required_capabilities": [
        "system-installation",
        "multiple-installations",
        "local-repository",
        "vendor-preinstall"
      ],
      "description": "Download vendor refs without deployment, then verify cached content through ordinary offline installation.",
      "preconditions": "Fresh stores, vendor app/runtime definitions and local fixture origin.",
      "expected": "Preinstall download-only leaves installed list empty; ordinary install --no-pull then deploys exact cached commits after origin URL becomes unavailable.",
      "references": [
        "doc/flatpak-preinstall.xml",
        "doc/flatpak-install.xml"
      ]
    },
    {
      "id": "maintenance.preinstall-sync",
      "scenario": "maintenance-preinstall-sync",
      "handler": "maintenance_scenarios:run",
      "profile": "system",
      "execution_environment": "root-userns",
      "drivers": [
        "cli"
      ],
      "required_capabilities": [
        "system-installation",
        "multiple-installations",
        "local-repository",
        "vendor-preinstall"
      ],
      "description": "Synchronize default vendor definitions through install, removal and re-addition.",
      "preconditions": "Fresh private stores and one documented .preinstall file naming the app and runtime.",
      "expected": "Default synchronization installs both refs; Install=false removes only the app; restoring Install=true reinstalls it.",
      "references": [
        "doc/flatpak-preinstall.xml"
      ]
    },
    {
      "id": "maintenance.preinstall-optout",
      "scenario": "maintenance-preinstall-optout",
      "handler": "maintenance_scenarios:run",
      "profile": "system",
      "execution_environment": "root-userns",
      "drivers": [
        "cli"
      ],
      "required_capabilities": [
        "system-installation",
        "multiple-installations",
        "local-repository",
        "vendor-preinstall"
      ],
      "description": "Contrast manual-uninstall opt-out with a fresh named installation using identical definitions.",
      "preconditions": "App and runtime successfully preinstalled, then app manually uninstalled from default.",
      "expected": "Named preinstall installs the app; default preinstall leaves it absent and preserves the runtime.",
      "references": [
        "doc/flatpak-preinstall.xml"
      ]
    }
  ],
  "mappings": [
    {
      "behavior_id": "maintenance.preinstall-download",
      "driver": "cli",
      "profile": "system",
      "requirements": [],
      "rationale": "Paired download/deploy phases observe no deployments followed by exact cached deployments while the origin is unavailable.",
      "surface_assertions": [
        {
          "id": "cli.option.preinstall.--no-deploy",
          "rationale": "Successful download leaves the fresh installed set empty but supplies content for subsequent ordinary offline installation."
        }
      ]
    },
    {
      "behavior_id": "maintenance.preinstall-sync",
      "driver": "cli",
      "profile": "system",
      "requirements": [
        "cli.preinstall.system.synchronize"
      ],
      "rationale": "No action flags: exact public ref sets and commits show installation, vendor removal, and successful re-addition."
    },
    {
      "behavior_id": "maintenance.preinstall-optout",
      "driver": "cli",
      "profile": "system",
      "requirements": [
        "cli.preinstall.system.opt-out"
      ],
      "rationale": "An actionable unchanged definition installs in fresh alt but does not undo manual default uninstall.",
      "surface_assertions": [
        {
          "id": "cli.option.preinstall.--user",
          "rationale": "User sync installs the app in the previously empty user store while default remains opted out and alt stays installed."
        },
        {
          "id": "cli.option.preinstall.--installation",
          "rationale": "Named sync deploys the app in fresh alt while the manually removed default copy stays absent."
        }
      ]
    }
  ],
  "notes": [
    "Uses system_selector_scenarios.Namespace with two disposable root user namespace layers and fixture-owned preinstall.d configuration. No non-root polkit or multiuser claim.",
    "Only public .preinstall definitions, CLI queries and fixture payloads at publicly returned deployment locations are inspected. No installed repository backend paths are constructed.",
    "The preinstall manual in this checkout is doc/flatpak-preinstall.xml. Install=false explicitly removes the app; the runtime remains designated for installation.",
    "History uses an actual systemd-journald process and journalctl --sync within one guarded namespace invocation. Each nested CLI argv, exit status, stdout and stderr is retained as JSON lines in outer command evidence. User history refers to namespace root's per-user installation, not an ordinary user's authorization behavior. Lifecycle checks require ordered installation/update/removal concepts for controlled refs, accepting noun/verb aliases and an optional deployment prefix. Unknown auxiliary labels and repeated events do not satisfy missing lifecycle steps and do not impose fixed record counts. The documented installation column must identify the selected store; auxiliary records may identify temporary repository paths. Pure helper controls accept extra auxiliary records and reject wrong selectors, missing steps, wrong order and swapped columns.",
    "Repair adds a third disposable user namespace mapping actual UID/GID 1000 to enclosing namespace UID/GID 0. Both callers query the same populated default system installation without installation/config redirects. Exact ref/commit checks bracket authorized root repair, which may rebuild filesystem objects. Device/inode identities at public installation/deployment paths are captured after root repair and must match across the non-root namespace and its read-only repair. The inherited host root remains read-only and no host bus is present. Coverage is limited to actual non-root refusal to mutate, including automatic dry-run; repository-object recovery and dry-run inconsistency detection remain unmapped.",
    "Preinstall --system receives no option assertion because ignoring it still selects the default system installation. No preinstall --no-pull assertion is claimed: the attempted cached setup returned Nothing to do even without that flag. Download-only is verified by empty installed queries and subsequent ordinary offline installation of its downloaded content."
  ]
}
