{
  "schema": 1,
  "behaviors": [
    {
      "id": "permissions.reset-app",
      "scenario": "permissions-reset-app",
      "handler": "permission_scenarios:run",
      "drivers": [
        "cli"
      ],
      "profile": "any",
      "required_capabilities": [
        "cli",
        "desktop-portals",
        "session-bus"
      ],
      "description": "Reset an application's permissions across tables.",
      "preconditions": "Two apps across three objects in two tables.",
      "expected": "Every object loses the subject's grants, retains the control's grants and data, and CLI app queries agree.",
      "references": [
        "doc/flatpak-permission-reset.xml"
      ]
    },
    {
      "id": "permissions.reset-all",
      "scenario": "permissions-reset-all",
      "handler": "permission_scenarios:run",
      "drivers": [
        "cli"
      ],
      "profile": "any",
      "required_capabilities": [
        "cli",
        "desktop-portals",
        "session-bus"
      ],
      "description": "Contrast app-scoped reset with --all.",
      "preconditions": "Two apps across three objects in two tables.",
      "expected": "App reset retains control grants; --all then empties permission maps on every object while retaining data and leaves no control-app query rows.",
      "references": [
        "doc/flatpak-permission-reset.xml"
      ]
    }
  ],
  "mappings": [
    {
      "behavior_id": "permissions.reset-app",
      "driver": "cli",
      "profile": "any",
      "requirements": [
        "cli.permission-reset.any.app"
      ],
      "rationale": "Independent Lookup on every seeded object proves all subject grants removed across tables, with control grants preserved; CLI app queries corroborate."
    },
    {
      "behavior_id": "permissions.reset-all",
      "driver": "cli",
      "profile": "any",
      "requirements": [],
      "rationale": "Option-only contrast of app-scoped and global resets.",
      "surface_assertions": [
        {
          "id": "cli.option.permission-reset.--all",
          "rationale": "App reset leaves control grants; --all removes those remaining grants on every object, so dropping or app-scoping the option fails."
        }
      ]
    }
  ],
  "notes": [
    "Uses a real xdg-permission-store on the case bus with isolated XDG_DATA_HOME; gdbus provides independent public fixture setup and mutation observations. No FUSE or document portal is needed.",
    "All service output and termination status are retained, including failed cases. Fixed catalogues and denominators are unchanged."
  ]
}
